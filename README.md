# gras-tool
AI tool for improving, expediting, and evaluating FDA GRAS filings

## Setup
1. Clone the repo
2. Create a .env file with ANTHROPIC_API_KEY and OPENAI_API_KEY
3. Run `python -m venv venv` and activate it
4. Run `pip install -r requirements.txt`
5. cd frontend && npm install
